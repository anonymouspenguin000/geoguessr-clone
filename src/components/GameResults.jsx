function GameResults({ map, guessPos, realPos, time, date }) {
    return (
        <div>
            <div style={{width: '500px', height: '500px'}}>{map}</div>
        </div>
    );
}

export default GameResults;
